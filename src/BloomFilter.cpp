// BloomFilter.cpp now only provides a translation unit; the BloomFilter class
// moved to BloomFilter.hpp. This file intentionally left minimal so the
// experimental driver can be implemented separately (TestBloom.cpp).

#include "BloomFilter.hpp"
